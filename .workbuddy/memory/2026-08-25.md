# 2026-08-25

- 分析并解读了 `web-file-automation-workbuddy.zip` 中的 WorkBuddy Skill：网页文件自动化（Playwright + 确认令牌 + 适配器契约）。
- 按用户要求移除了店铺销量处理工具页签①中的「默认规则」与「默认运营级别」两个字段：
  - 修改 `store-sales-processor.html`，删除 UI 字段及关联 JS 逻辑；
  - `buildRulesCfg()` 中强制 `default_rule` 与 `default_op_level` 为空，未绑定/未映射的运营级别不再走兜底；
  - 已通过 HTML/JS 语法检查，提交并推送至 GitHub（commit 6e29cfa），Render 将自动重新部署。
- 针对页签②「FBA 需求差异分析」出现「处理失败：0」的问题进行排查：
  - 该提示不是服务端业务校验错误（缺列、权重为 0 等会返回中文说明），而是网络/服务级异常或实例被中断的特征；
  - 已增强诊断：后端任务异常时把 traceback 存入任务状态，前端轮询到 error 时展开显示；
  - 修改 `server.py` 与 `store-sales-processor.html`，提交推送（commit 74b8905），Render 自动部署。
- 按用户要求将页面标题、网页名称、页脚统一改为「FBA需求分配核查工具」；同步更新 README.md 标题；提交推送（commit 62b2b8c）。
- 定位到页签②「处理失败：0」的根因：前端状态轮询每秒 1 次，触发 Cloudflare 429 Too Many Requests 限流，导致后续轮询全部失败。
  - 修复：所有状态轮询间隔从 1 秒调整为 5 秒；轮询遇到 429 时静默重试，遇到其他非 200 状态码则停止并提示 HTTP 状态；
  - 同步给 eu_detail 明细生成轮询也加上 429/非 200 处理；
  - 提交推送（commit dedbc5b），Render 自动部署。
