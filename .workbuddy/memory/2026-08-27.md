# 2026-08-27

- 将默认销量加权占比从 60%/30%/10% 改为 **30%/50%/20%（3天/7天/14天）**：
  - 后端 `server.py` 的 `DEF = (0.3, 0.5, 0.2)`（兜底，仅当前端未传 w3/w7/w14 时生效）；
  - 前端页签①权重输入框默认值 `value` 改为 30/50/20，下拉「默认」预设 `PRESETS.default` 改为 [30,50,20]，选项标签与说明文字同步；
  - `process_sales.py` 文档注释同步；
  - 逻辑不变：页签② **未绑定规则（legacy）** 用该默认比例算加权日均；**已绑定规则（rules）** 每个运营级别用自己的绑定规则比例（已是按级设置）；未绑定规则的运营级别判不满足条件；
  - 本地合成数据验证：legacy 0.3*100+0.5*200+0.2*300=190.0，rules(R14=14天权重1) =300.0，均通过；
  - 提交推送（commit 6cbd890），Render 自动部署。
- 用户再次反馈线上 `{"error": "task not found"}`：
  - 实测线上 `https://store-sales-processor.onrender.com` 健康（/api/ping=200、/api/tasks={"tasks":[]}、两次 ping 稳定 0.5~0.8s，无崩溃循环）；
  - 根因：用户当前标签页是部署(6cbd890)前就开着的旧页面，旧任务的轮询定时器在我部署重启服务端、清空内存 TASKS 后仍在查询已不存在的任务 id；前端 `taskId` 等仅存内存、刷新即清空，且无 on-load 自动轮询，故全新加载不会触发；
  - 改进：四个轮询函数（页签①poll / fbaPoll / fbaPoll / invPoll / allocPoll）在收到 404 时检测 `task not found` 并显示友好提示「服务端可能已重启/重新部署，内存中的任务已清空，请刷新页面后重新运行」并停止轮询；
  - 提交推送（commit 35fa1ef），Render 自动部署；
  - 处置：用户强刷即可；若想根治「重启丢任务」，可做 TASKS 磁盘持久化（Render 免费实例同容器重启可恢复，跨部署新容器磁盘重置则不行）——已列为可选方案，用户此前倾向「暂时先不改」。
