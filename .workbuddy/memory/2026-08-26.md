# 2026-08-26

- 修复页签②「FBA 需求差异分析」在 rules 模式下未按运营级别文件筛选的问题：
  - 根因：rules 模式下 `process_ops` 完全依赖「运营级别映射表」判断每个 ASIN 的运营级别，但页签②没有映射表上传入口，导致所有 ASIN 都走 default_op_level（空字符串），被判定为「未绑定规则/运营级别未选」；
  - 修复：`process_ops` 改为优先读取运营级别文件本身的「运营级别」列，列值为空时才 fallback 到映射表 / default_op_level；
  - `level_info` 增加 `mode` 字段供前端区分 rules/legacy；
  - 前端：`rulesModeEl` 开启后禁用页签②的「符合补货条件的运营级别」输入框并显示提示；汇总区 rules 模式显示「页签①已绑定运营级别」与「文件中未绑定的运营级别」提示，避免误以为输入框还能筛选；
  - 已通过 `_test_fba_rules.py`、`_test_weighted_rules.py`、`_e2e_fba_http.py` 全部测试；
  - 提交推送（commit 7c03b37），Render 自动部署。
- 用户反馈页签①已绑定三个运营级别，但页签②汇总仍显示 legacy 文案、未识别绑定：
  - 根因：`rulesMode` 备货规则开关状态未持久化，页面刷新/重进后开关恢复未勾选，`buildRulesCfg()` 不被调用，页签②请求未携带 `cfg`，后端按 legacy 模式处理；
  - 修复：新增 `amz_rules_mode_enabled` localStorage 项保存开关状态；页面初始化时恢复开关并同步 UI；导入 rules 配置时自动开启开关；
  - 已通过 HTML/JS 语法检查、`_test_fba_rules.py`、`_e2e_fba_http.py` 全部测试；
  - 提交推送（commit 7bde22e），Render 自动部署。
