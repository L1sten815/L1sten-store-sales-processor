# 2026-08-26

- 修复页签②「FBA 需求差异分析」在 rules 模式下未按运营级别文件筛选的问题：
  - 根因：rules 模式下 `process_ops` 完全依赖「运营级别映射表」判断每个 ASIN 的运营级别，但页签②没有映射表上传入口，导致所有 ASIN 都走 default_op_level（空字符串），被判定为「未绑定规则/运营级别未选」；
  - 修复：`process_ops` 改为优先读取运营级别文件本身的「运营级别」列，列值为空时才 fallback 到映射表 / default_op_level；
  - `level_info` 增加 `mode` 字段供前端区分 rules/legacy；
  - 前端：`rulesModeEl` 开启后禁用页签②的「符合补货条件的运营级别」输入框并显示提示；汇总区 rules 模式显示「页签①已绑定运营级别」与「文件中未绑定的运营级别」提示，避免误以为输入框还能筛选；
  - 已通过 `_test_fba_rules.py`、`_test_weighted_rules.py`、`_e2e_fba_http.py` 全部测试；
  - 提交推送（commit 7c03b37），Render 自动部署。
- 用户反馈页签①已绑定三个运营级别，但页签②汇总仍显示 legacy 文案、未识别绑定：
  - 根因：`rulesMode` 备货规则开关状态未持久化，页面刷新/重进后开关恢复未勾选，`buildRulesCfg()` 不被调用，页签②请求未携带 `cfg`，后端按 legacy 模式处理；
  - 修复：新增 `amz_rules_mode_enabled` localStorage 项保存开关状态；页面初始化时恢复开关并同步 UI；导入 rules 配置时自动开启开关；
  - 已通过 HTML/JS 语法检查、`_test_fba_rules.py`、`_e2e_fba_http.py` 全部测试；
  - 提交推送（commit 7bde22e），Render 自动部署。
- 完成页签③改为「总可用库存核对」：
  - 功能：上传 FBA 需求表 + 库存表，按 SKU编码 核对 FBA 需求表里的 本地SKU总可用库存 与 库存表按 SKU 汇总的 可用库存 是否一致；
  - 修复下载响应头中文文件名导致 UnicodeEncodeError：Content-Disposition 使用 ASCII-safe 的 legacy filename + RFC 5987 `filename*` 百分号编码；
  - 修复前后端槽位名不一致：前端上传 `slot=fba/stock`，后端原按 `inv_fba/inv_stock` 读取，已统一为 `fba/stock`；
  - 端到端测试通过（1866 一致 / 6 个 FBA SKU 库存表缺失 / 142124 个库存独有 SKU）；
  - 提交推送（commit e0bae6d），Render 自动部署。
- 排查页签②「FBA需求差异分析」189 条"多余"差异根因：
  - 用三个文件本地复跑 `process_ops`+`process_diff`（legacy 默认：rules 关、levels 空、权重 0.6/0.3/0.1），正好复现 extra=189，与线上一致；
  - 结论：这 189 条全部是欧洲站 ASIN，匹配键 `店铺ASIN` 完全一致（去空白/大小写后 0 条能匹配，排除键格式问题）；
  - 全部落在"FBA需求有、运营满足条件=否"；进一步定位：销售状态=正常销售、配送类型=FBA，但 `销量.xlsx` 里这些 ASIN 的 三天/七天/十四天日均销量 全是 0（仅 30 天日均有 0.17 微弱值）→ 加权日均 L=0 → 不满足 `L>0` 条件；
  - 根因：差异并非 bug 或键错配，而是这些欧洲站 ASIN 近 3/7/14 天几乎无销量，工具据此不生成补货，而 FBA需求表仍包含它们，故被标"多余"。
- 排查线上"网页进不去 / task not found"报错：
  - 远端 `https://store-sales-processor.onrender.com` 实测健康：/api/ping=200、首页=200；
  - 用真实大文件在线上复跑两个页签均成功：页签②(销量15MB+运营级别4.6MB+FBA需求0.6MB) status=done 汇总 extra=189/miss=18468/both=1805；页签③(库存4MB/144k行) status=done 1872 SKU/1866 一致；
  - 结论：非代码 bug；`task not found` 来自后端 `GET /api/tasks/<id>` 内存字典查不到——即"建任务后服务端进程在轮询前重启"（部署重启 / Render 免费实例空闲休眠后冷启动 30~60s），内存里的 TASKS 被清空；
  - 处置：服务现已稳定，用户强刷即可；若反复出现可考虑把 TASKS 持久化到磁盘或升级 Render 实例（常驻/更大内存）。
- 修复页签② rules 模式下绑定运营级别后"运营满足条件=0"的 bug：
  - 根因：`process_ops` 的 sales 循环在 rules 模式下依赖 `mapping` 表给每个 ASIN 预分配运营级别，只累加该级别绑定规则的加权销量；但页签②没有 mapping 上传入口，导致 `sales_sums` 为空，ops 输出循环虽能从运营级别文件列读到正确级别并找到绑定规则，`M_rule` 却为 0，所有 ASIN 被判不满足；
  - 修复：删除 sales 循环里的 `sales_sums` 预累加逻辑；改为在 ops 输出循环里直接用 `sales_6win`（6 窗口汇总）按规则计算 `M_rule`；`rule_plans` 也改为按窗口编号存储，不再依赖销量表列下标；
  - 本地回归：`_test_fba_rules.py`、`_test_weighted_rules.py` 全部通过；用用户三份真实文件+绑定 新品/欧洲-30天日均-[0~1]/美国-30天日均-[0~5] 复跑，`op_meet` 从 0 变为 2151，`extra` 从 5636 降至 3770；
  - 提交推送（commit ef131c8），Render 自动部署。
- 页签②上传区加红字提示（用户要求）：
  - 在「销量原始文件」与「运营级别文件」两个上传区下方各加一行红字：「⚠ 文件只放正常销售的数据即可，文件太大会导致服务器崩溃！」；
  - 仅前端改动（store-sales-processor.html），无后端逻辑变化；
  - 提交推送（commit 222f74c），Render 自动部署。
